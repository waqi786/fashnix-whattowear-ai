<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="12dp">

    <!-- User Message (Right Side) -->
    <LinearLayout
        android:id="@+id/userMessageLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginStart="64dp"
        android:orientation="vertical"
        android:gravity="end"
        android:paddingEnd="16dp">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/userImage"
            android:layout_width="240dp"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:visibility="gone"
            app:shapeAppearanceOverlay="@style/RoundedTopCornersLarge"
            android:layout_marginBottom="8dp"
            app:strokeColor="@color/primary"
            app:strokeWidth="1.5dp" />

        <com.google.android.material.card.MaterialCardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardBackgroundColor="@color/primary"
            app:cardCornerRadius="32dp"
            app:cardElevation="0dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearance_UserBubble">

            <TextView
                android:id="@+id/userText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="20dp"
                android:paddingVertical="14dp"
                android:text="Style this for a night event."
                android:textColor="@color/text_primary"
                android:textSize="15sp"
                android:fontFamily="sans-serif-medium" />
        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>

    <!-- AI Stylist Message (Left Side) -->
    <LinearLayout
        android:id="@+id/aiMessageLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:layout_marginEnd="64dp"
        android:orientation="horizontal"
        android:gravity="bottom"
        android:layout_marginTop="12dp"
        android:paddingStart="16dp">

        <com.google.android.material.imageview.ShapeableImageView
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginEnd="12dp"
            android:src="@drawable/ic_sparkle"
            android:padding="8dp"
            android:background="@color/surface_glass"
            app:shapeAppearanceOverlay="@style/CircleImageView"
            app:strokeColor="@color/border_glass"
            app:strokeWidth="1.5dp" />

        <com.google.android.material.card.MaterialCardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardBackgroundColor="@color/surface_glass"
            app:strokeColor="@color/border_glass"
            app:strokeWidth="1.5dp"
            app:cardCornerRadius="32dp"
            app:cardElevation="0dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearance_AiBubble">

            <TextView
                android:id="@+id/aiText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="20dp"
                android:paddingVertical="14dp"
                android:text="I recommend pairing this with your black silk trousers."
                android:textColor="@color/white"
                android:textSize="15sp"
                android:lineSpacingExtra="6dp" />
        </com.google.android.material.card.MaterialCardView>
    </LinearLayout>

</LinearLayout>
